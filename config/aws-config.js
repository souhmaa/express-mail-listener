(function () {

    'use strict';
    
    var awsConfig = {
        credentials: {
            accessKeyId: "ACCESS_KEY_ID",
            secretAccessKey: "ACCESS_SECRET_ID"
        },

        bucketname: "BUCKET_ID"
    }

    module.exports = awsConfig;

})();